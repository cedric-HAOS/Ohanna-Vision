# Intégration dans Ohanna-Vision

Copier le contenu de :

`src/ohanna_vision/web/static/`

dans le répertoire équivalent du dépôt.

Ajouter les balises favicon et manifest dans `index.html`, puis compléter
les `package-data` du `pyproject.toml` comme indiqué dans `BRAND-GUIDELINES.md`.
